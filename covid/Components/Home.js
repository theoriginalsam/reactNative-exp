import React from "react";
import { StyleSheet, View, Text } from "react-native";
import Screen from './Screen/search'

const Home = () => {
    return (<View>
        <Screen />
    </View>)
};

const styles = StyleSheet.create({});

export default Home;
