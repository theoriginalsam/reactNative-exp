import React from 'react';
import {StyleSheet, View, Text} from 'react-native';

const ResturantType = () => {
  return (
    <View>
      <Text>Title</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ResturantType;
